%% Convert To Discrete
% this option converts the designed controller to discrete, using matched
% method. the user should select the time sample, and the anti-aliasing
% filter, if exist. The input should be the anti-aliasing filter name, as saved in the workspace.